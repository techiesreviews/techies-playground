# Release 0.8.0 preparation

- Date: 2026-09-16
- Owner: Codex
- Status: complete
- Disposition: kept
- Related version: 0.8.0
- Related commit/issue: release commit containing this record

## Context

The user requested deployment of [page-wide package drops](2026-09-16-page-wide-package-drops.md). This release supersedes that record's unreleased status without changing its test outcomes.

## Hypothesis and success criteria

Publish the verified feature through the existing main-branch GitHub pipeline with matching package, lockfile and user-facing changelog versions.

## Options considered

Use the repository's GitHub pipeline (chosen); direct Cloudflare publication is excluded by repository instructions.

## Implementation or prototype

Bumped package and lockfile versions to 0.8.0 and added release notes for mixed ZIP drops, automatic routing, upload feedback and exclusive theme selection. Updated CONTEXT and the change index. Main and origin/main matched before preparation; the deployment gate was already enabled.

## Tests and evidence

| Environment | Procedure/command | Expected | Actual | Evidence |
| --- | --- | --- | --- | --- |
| Node 22.22.1 | `npm test` | All tests pass | 62 passed, 0 failed | `/tmp/techies-release-tests.log` |
| Vite / Wrangler | `npm run deploy:check` | Build and deployment dry run pass | Passed; build large-chunk warning only | `/tmp/techies-release-check.log` |
| Workspace | Version assertion and `git diff --check` | Matching 0.8.0 versions, clean whitespace | Passed before commit | Release preparation command |

## Result

Release preparation passed. GitHub execution and live-domain verification will be recorded separately after pushing.

## Disposition and rationale

Kept: release metadata and local deployment checks are complete.

## Documentation impact

Updated CONTEXT, change index, package metadata and CHANGELOG_ENTRIES. Feature implementation already updates architecture, rebuild specification, data contracts and testing documentation.

## Follow-ups and open questions

Verify pipeline completion and matching live assets after push. Browser visual and cross-origin iframe-first drag limitations remain as documented in the feature record.
