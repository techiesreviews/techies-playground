# Release 0.6.0 publication

- Date: 2026-08-28
- Owner: Codex publication pass
- Status: complete
- Disposition: kept
- Related version: 0.6.0
- Related commit/issue: release commit on `main`

## Context

The working tree combines a site-styled GitHub Star control, featured-first plugin discovery, consistent plugin selection cards/check-circles, and simplified uploaded-plugin metadata. The user requested that the full local result be documented, added to the changelog, and deployed through the repository's normal `main`-branch pipeline.

## Hypothesis and success criteria

Release 0.6.0 is ready when every pending user-visible change is represented in the in-app changelog and current-state documentation, package/changelog versions agree, automated tests and the Cloudflare dry run pass, the intended files are committed to `main`, `origin/main` receives the commit, and the GitHub deployment plus live domain are verified.

## Options considered

- Publish directly with Wrangler. Rejected because repository policy defines deployment as a push to `main` through GitHub Actions.
- Patch release 0.5.1. Rejected because the release adds a new repository-stars capability and materially expands plugin discovery/selection UI.
- Minor release 0.6.0 through the existing pipeline. Selected because the changes are backward-compatible product features and refinements.

## Implementation or prototype

- Added the 0.6.0 in-app changelog entry covering all relevant changes since 0.5.0.
- Bumped `package.json`, both root lockfile version fields, and the current-state version reference to 0.6.0.
- Linked the release's experiment records from the change index and associated them with 0.6.0.
- Updated current-state product, architecture, data-contract, rebuild, testing, and history documentation for the final implementation.

## Tests and evidence

| Environment | Procedure/command | Expected | Actual | Evidence |
| --- | --- | --- | --- | --- |
| Local release metadata | Node consistency script and `git diff --check` | Package, lockfile root, lockfile package, and newest changelog versions all equal 0.6.0; diff has no errors | Passed; all four values were `0.6.0`; only existing LF-to-CRLF working-copy warnings were printed | Executed 2026-08-28 |
| Windows, Node 24.15.0 | `npm test` | All contract tests pass | 50 passed, 0 failed, in 226 ms | Executed 2026-08-28 |
| Windows, Node 24.15.0 | `npm run deploy:check` | Production build and Wrangler dry run succeed without publishing | Build completed in 4.54 s; Wrangler 4.122.0 read 10 assets and exited with `--dry-run` | Executed 2026-08-28 |
| Windows, Node 22 via `npx -y node@22` | Node test, Vite build, then Wrangler deploy dry run | Match the GitHub Actions Node major and complete the full release gate | 50 passed, 0 failed, in 236 ms; build completed in 5.14 s; Wrangler read 10 assets and exited with `--dry-run` | Executed 2026-08-28 |

## Result

All pre-push release requirements passed. The first ad-hoc PowerShell lockfile parser could not process the large lockfile; the authoritative Node JSON check was then run successfully and verified all four release versions. No direct Cloudflare deployment was performed.

## Disposition and rationale

Kept and approved for publication as 0.6.0 through the existing GitHub `main` deployment pipeline.

## Documentation impact

Updated the complete current-state documentation set, experiment index, user-facing changelog, and package versions for 0.6.0. This record documents the publication pass.

## Follow-ups and open questions

- Verify the GitHub Actions deployment and `https://play.techies.tools` after pushing `main`.
