# Release 0.7.1 deployed

- Date: 2026-09-10
- Owner: Codex
- Status: complete
- Disposition: kept
- Related version: 0.7.1
- Related commit/issue: [a230a1e](https://github.com/techiesreviews/techies-playground/commit/a230a1e0a60f152fc0145aa6108be662acd8234f)

## Context

The user authorized fixes and deployment following the [security assessment](2026-09-10-security-review.md). Implementation and local validation are recorded in [the remediation record](2026-09-10-security-fixes.md).

## Hypothesis and success criteria

The GitHub pipeline must test/build/deploy the fixed commit, and the live domain must serve the matching 0.7.1 application with usable directory search and the configured security headers.

## Options considered

- Direct Cloudflare publishing: excluded by the repository deployment contract.
- Push main and use the existing GitHub workflow: selected.

## Implementation or prototype

Committed and pushed a230a1e to main. Git initially lacked an author identity; configured only this repository using the verified GitHub account identity already present in recent commits. The push-triggered workflow was skipped because `CLOUDFLARE_DEPLOY_ENABLED` was false. Actor/repository gates matched, and the account variable and API-token secret existed. Enabled the repository deployment variable and dispatched the existing workflow at main under the user's deployment authorization. No direct Wrangler deployment was used.

## Tests and evidence

| Environment | Procedure/command | Expected | Actual | Evidence |
| --- | --- | --- | --- | --- |
| GitHub | Inspect initial push run | Deploy fixed commit | Skipped by disabled deployment variable | [Skipped run](https://github.com/techiesreviews/techies-playground/actions/runs/34455312736) |
| GitHub | `gh workflow run deploy.yml --ref main`; inspect run and jobs | All pipeline steps pass | Install, tests, build, Cloudflare deploy and completion succeeded for a230a1e | [Successful run](https://github.com/techiesreviews/techies-playground/actions/runs/34455348719) |
| GitHub | Query open Dependabot alerts after push | No remaining open alerts | Empty list | `gh api repos/techiesreviews/techies-playground/dependabot/alerts` filtered to state=open |
| Production | `curl -sSI --max-time 20 https://play.techies.tools` | HTTP 200 and configured headers | HTTP 200, CSP, Permissions-Policy, no-referrer, nosniff and SAMEORIGIN | Observed 08:29:16 UTC |
| Production/local build | Fetch HTML and JS with curl; compare JS bytes to dist | Deployed bundle equals verified local build | Exact byte match; version 0.7.1 present | `/assets/index-O4Jho3bU.js`, SHA-256 `a3426ff56be4df57e33a1f75987ac02bdf39eba3d2f32dcc81ccf0ee36cb33a5` |
| Chromium, live site | Open changelog; search plugins for woocommerce | Current release visible and directory works | 0.7.1 and “Safer recipes and license locking” visible; WooCommerce results returned | Browser evaluate result |

The first bundle-check attempt with Python urllib received HTTP 403; the equivalent curl requests succeeded and supplied the byte-comparison evidence above. The git push printed a stale vulnerability-count warning; the subsequent authenticated Dependabot query returned no open alerts.

## Result

The security release is live at https://play.techies.tools. The deployed bundle matches the tested local production build.

## Disposition and rationale

Kept. All required release checks passed. This documentation-only follow-up records completed evidence without changing the deployed application or version.

## Documentation impact

Added this deployment record and indexed it. The 0.7.1 changelog and package/lockfile versions were already committed with the security fixes.

## Follow-ups and open questions

The remediation record lists the remaining scope limits and optional hardening suggestions. No new product changes are included in this deployment-evidence commit.
