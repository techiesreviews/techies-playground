# Release 0.5.0 publication

- Date: 2026-08-28
- Owner: Codex publication pass
- Status: complete
- Disposition: kept
- Related version: 0.5.0
- Related commit/issue: release commit on `main`

## Context

The working tree combined automatic WordPress stable-release synchronization, featured WordPress.org plugin discovery, safer directory-name rendering, and the new AI-oriented project documentation system. The user requested that the complete result be put on GitHub, which invokes the repository's normal `main`-branch deployment workflow.

## Hypothesis and success criteria

Release 0.5.0 is ready when all pending product and documentation changes are represented in the user-facing changelog, version values agree, tests and the production build pass, the commit reaches `origin/main`, and the GitHub deployment succeeds.

## Implementation

- Added the daily WordPress stable-release synchronization workflow and tested update generator.
- Added featured WordPress.org plugin cards and decoded directory-name punctuation.
- Added the complete reconstruction documentation and durable change/experiment ledger.
- Expanded the 0.5.0 in-app changelog to cover every relevant pending change.

## Tests and evidence

| Environment | Procedure/command | Expected | Actual | Evidence |
| --- | --- | --- | --- | --- |
| Windows, Node 22.23.2 | `npx -y node@22 --test` | All contract tests pass. | 48 passed, 0 failed. | Publication terminal session on 2026-08-28. |
| Windows, Node 22.23.2, Vite 7.3.6 | `npx -y node@22 node_modules/vite/bin/vite.js build` | Vite build succeeds. | Build completed in 6.61 seconds; Vite reported the existing large-chunk warning. | Publication terminal session on 2026-08-28. |
| Windows, Node 22.23.2, Wrangler 4.122.0 | `npx -y node@22 node_modules/wrangler/bin/wrangler.js deploy --dry-run` | Cloudflare Worker bundle and asset binding validate without deployment. | Dry run succeeded and read 10 built asset files. | Publication terminal session on 2026-08-28. |
| Windows, repository working tree | `git diff --check` and version/changelog consistency check | Clean diff and one matching version. | Diff check passed; package, lockfile, lockfile root, and changelog all report 0.5.0. | Publication terminal session on 2026-08-28. |

## Result

All required pre-push verification passed on the same Node major version used by GitHub Actions. The release is ready for the normal `main`-branch deployment pipeline.

## Disposition and rationale

Keep and publish as 0.5.0 through the GitHub deployment pipeline.

## Documentation impact

- Updated the in-app 0.5.0 changelog.
- Added this release record to the change ledger.

## Follow-ups and open questions

- Verify the GitHub deployment run and `https://play.techies.tools` after pushing `main`.
