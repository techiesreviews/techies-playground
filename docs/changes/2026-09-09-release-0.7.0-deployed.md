# Release 0.7.0 deployed and live-verified

- Date: 2026-09-09
- Owner: Codex
- Status: complete
- Disposition: kept
- Related version: 0.7.0
- Related commit/issue: `9b93d28f5468ac77a8eab62fe5b40ddd2390be79`; [successful deployment](https://github.com/techiesreviews/techies-playground/actions/runs/34385196327)

## Context

The user requested publication to play.techies.tools and a shorter preview header. This record completes the live verification follow-up from [release preparation](2026-09-09-release-0.7.0.md).

## Hypothesis and success criteria

The GitHub pipeline should publish 0.7.0, with the live domain serving its bundle and supporting actual Ctrl-click Site/View navigation with the compact 29 px reminder.

## Options considered

- Direct Cloudflare deployment: outside the repository deployment contract.
- Temporarily enable and dispatch the existing GitHub workflow: selected, following the previous release procedure.

## Implementation or prototype

Pushed release commit `9b93d28` to main. The push-triggered run `34385158497` was skipped because `CLOUDFLARE_DEPLOY_ENABLED` was false. Confirmed the token secret's name existed without reading it, temporarily enabled the variable and dispatched `deploy.yml` on main. After successful completion, restored the variable to false and verified its value through the GitHub API. No workflow code changes or direct deployments were used.

## Tests and evidence

| Environment | Procedure/command | Expected | Actual | Evidence |
| --- | --- | --- | --- | --- |
| GitHub Actions | `gh run watch 34385196327 --exit-status --interval 10` | All deployment steps pass | Successful in 34 seconds, including tests, build and Cloudflare publish | Run linked above |
| Repository variable | Read `CLOUDFLARE_DEPLOY_ENABLED` through `gh api` | Original disabled state restored | `false` | Executed after deployment |
| Live HTTP | Fetch cache-busted homepage and referenced JavaScript | HTTP 200 and 0.7.0 release markers | HTTP 200; `/assets/index-hXE4ozcH.js` contains version and release title | Executed Node fetch verification |
| Live response | Inspect Content-Security-Policy | Existing restricted frame/script policies remain | `frame-src https://playground.wordpress.net`; `script-src 'self'` | Live header inspection |
| Chromium 153 / live domain | Launch empty temporary site; Ctrl-click admin-bar Site; wait for Hello world! heading | Homepage opens in preview tab | Passed; reminder measured 29 px | Browser-driven Playwright click with Control modifier |
| Same | Pages → Sample Page → Ctrl-click View; wait for Sample Page heading | Page opens in preview tab | Passed | Browser-driven Playwright click with Control modifier |
| Same | Close owner and discard temporary site | Preview explains unavailable runtime, iframe removed | Passed, iframe count 0 | Browser assertion; disposable context closed afterward |

## Result

Release 0.7.0 is live. Pipeline completion, bundle inspection and browser navigation independently confirmed the deployed behavior and compact header.

## Disposition and rationale

Kept: requested release is deployed and verified. The deployment gate was restored to its previous state.

## Documentation impact

Added this deployment verification and its index entry. Previous records remain unchanged. This documentation-only follow-up does not change the deployed application bundle.

## Follow-ups and open questions

- Firefox/Safari and unusual popup/context-menu flows retain the previously documented coverage limits.
- The Cloudflare action reports a Node 20 deprecation warning but deployment succeeded.
