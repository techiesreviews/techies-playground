# Release 0.7.0: separate preview tabs and compact reminder

- Date: 2026-09-09
- Owner: Codex
- Status: complete
- Disposition: kept
- Related version: 0.7.0
- Related commit/issue: deployment commit containing this record

## Context

The user authorized deployment of the previously tested preview-tab implementation to play.techies.tools and requested a shorter reminder bar. This release follows [preview implementation](2026-09-09-preview-tabs.md) and [actual WordPress link verification](2026-09-09-wordpress-link-verification.md), preserving their historical outcomes.

## Hypothesis and success criteria

Keep the same preview behavior while reducing the one-line reminder from 45 px to 29 px. Ship matching changelog and package versions through the existing GitHub deployment pipeline after tests and deployment dry run pass.

## Options considered

- Leave the 45 px header unchanged: does not address the annotation.
- Reduce vertical padding from 12 px to 4 px: selected; retains readable 14 px text and wrapping at narrow widths.

## Implementation or prototype

`src/Preview.jsx` uses `py-1` instead of `py-3`. The 0.7.0 changelog covers separate preview tabs, WordPress Site/View links, the compact reminder and unavailable-session feedback. Both package manifests and CONTEXT.md agree on 0.7.0. The rebuild specification records the header dimensions. All previously reviewed preview implementation and documentation changes are included.

## Tests and evidence

| Environment | Procedure/command | Expected | Actual | Evidence |
| --- | --- | --- | --- | --- |
| Node 22 | `npm test` | All tests pass | 55 passed, 0 failed | `/tmp/techies-release-tests.log` during release preparation |
| Vite / Wrangler | `npm run deploy:check` | Build and dry run pass | Build passed; Wrangler exited after dry run | `/tmp/techies-release-build.log`; no direct deployment |
| Chromium / production build | Open preview screen at 1744 px; measure header bounding box and computed padding | 29 px height, 4 px vertical padding | Matched | Playwright browser evaluation |
| Chromium / production build | Resize to 320 px; compare document width and viewport | No horizontal overflow | Both 320 px | Playwright browser evaluation |
| Working tree | Check package, lockfile/root package and newest changelog versions; `git diff --check` | All versions 0.7.0, clean whitespace | Passed | Release verification before push |

## Result

The compact header and release preparation checks passed. Earlier browser-driven Site/View Ctrl-click and owner-close tests remain applicable; only spacing and release metadata changed in this pass. GitHub deployment and live checks follow the release push and are reported separately.

## Disposition and rationale

Kept: fulfills the explicitly requested deployment and header refinement using the existing pipeline.

## Documentation impact

Updated current rebuild specification, CONTEXT.md, user-facing changelog, package versions and change index. Included preview architecture, data contract and test documentation from prior work.

## Follow-ups and open questions

- Verify the pushed commit's GitHub deployment and live domain.
- Cross-browser and unusual popup-flow limitations remain as recorded in the previous verification record.
